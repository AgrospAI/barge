This project is just for testing that the contract and subgraph deployment have been successful.

```python
uv run pytest
```

Creates a new MetadataRequest in the `development` network and queries it using `subgraph`.
